const assert = require('assert')
const deleteCouponsFromCart = require('../../../cart/deleteCouponsFromCart')

describe.skip('deleteCouponsFromCart', () => {
  it.skip('Should delete coupon from a cart', (done) => {
      done()
  })
})
