const assert = require('assert')
const addCouponsToCart = require('../../../cart/addCouponsToCart')

describe.skip('addCouponsToCart', () => {
  it.skip('Should add coupon to a cart', (done) => {
      done()
  })
})
